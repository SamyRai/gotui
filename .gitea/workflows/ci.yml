name: CI
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  go-ci:
    uses: mukimovd/.github/.gitea/workflows/go-ci.yml@main
    with:
      go-version: '1.25'
