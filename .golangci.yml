version: "1"
linters:
  enable:
    - govet
    - golint
    - gocyclo
    - dupl
    - goconst
    - gocritic

run:
  timeout: 5m
  tests: true
  issues-exit-code: 1

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
