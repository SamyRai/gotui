version: "2"
linters:
  enable:
    - govet
    - revive
    - gocyclo
    - dupl
    - goconst
    - gocritic

run:
  timeout: 5m
  tests: true
  issues-exit-code: 1

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
